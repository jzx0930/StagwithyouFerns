# 手動還原備份 / 封存(只留本機,不上傳)
_backup/

# 我測試/誤建的空資料夾(可在檔案總管刪)
_movetest/
新增資料夾/

# 未壓縮原始模型(只留本機備份,不上傳、不部署)
models/**/未壓縮/
models/**/*-original.glb
models/**/*.opt.glb

# 下載的優化工具(不該進 repo)
glb-optimization-master-main/
glb-optimization-master/

# 暫存預覽圖
_*_preview.png
models/**/_*_preview.png
models/**/*-poster.png
models/_poster_sheet.png

# 其他大型原始 .glb
models/**/Hitem3d-*.glb
models/**/Meshy_*.glb

# 子代理重建 data.json 的暫存檔(本機刪即可)
_new_plants.json

# 交接文件(依使用者要求只留本機、不推上庫;若已被追蹤需 git rm --cached SKILL.md 一次)
SKILL.md

# orb vendor 打包產物(執行 tools/orb-vendor/build.bat 產生;vendor/thinking-orbs.js 要 commit)
tools/orb-vendor/node_modules/
tools/orb-vendor/package.json
tools/orb-vendor/package-lock.json
