/* ===================================================================
   購物車 / 訂單設定  ——  這個檔案由你（賣家）自己編輯
   ===================================================================
   ✱ 這個檔「不會」被 Drive 重建 data.json 覆蓋,價格放這裡最安全。
   ✱ 改完存檔 → GitHub Desktop commit + push 就生效。
   =================================================================== */
window.SHOP_CONFIG = {

  // ── 總開關:要不要開放購買功能 ──
  // true = 開啟(顯示價格、加入購物車、結帳)
  // false = 關閉(整個購物功能都不出現,網站回到純展示)
  enabled: true,

  // ── Web3Forms access key(訂單會寄到這組 key 綁定的信箱)──
  // 到 https://web3forms.com 用你的 email 免費申請,把拿到的 key 貼在這裡。
  web3formsKey: "在這裡貼上你的 Web3Forms access key",

  // ── 幣別前綴 ──
  currency: "NT$",

  // ── 每株植物的價格(鍵 = 植物的「中文名」,要和 data.json 的 name 一致)──
  // 沒列在這裡的植物 → 顯示「價格洽詢」,仍可加入購物車(當作詢價)。
  // 範例:
  //   "白玫瑰": 1200,
  //   "史迪奇": 800,
  //   "奶油獅": 1500,
  prices: {

  }
};
